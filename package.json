{
  "name": "crowdloan",
  "version": "1.0.0",
  "description": "Crowdloan tracking on Kusama",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Hao Ding",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "latest",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "latest"
  },
  "resolutions": {
    "@polkadot/api": "latest",
    "@subql/node": "latest"
  },
  "dependencies": {
    "@subql/node": "latest"
  }
}
